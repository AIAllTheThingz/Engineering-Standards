# Distribution template only. GitHub does not execute reusable workflows from
# this root workflows/ directory. Copy this file into .github/workflows/ in a
# downstream repository or call:
# AIAllTheThingz/Engineering-Standards/.github/workflows/governance-ci-reusable.yml@<immutable-reference>
name: Governance

on:
  pull_request:
  push:
    branches:
      - master
      - main

permissions:
  contents: read

jobs:
  governance:
    name: Governance
    uses: AIAllTheThingz/Engineering-Standards/.github/workflows/governance-ci-reusable.yml@<immutable-reference>
    with:
      project-path: .
      governance-version: 1.1.0
