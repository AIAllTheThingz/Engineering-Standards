name: Candidate Governance Implementation

on:
  workflow_call:

permissions:
  contents: read

jobs:
  candidate:
    name: Candidate implementation validation
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    permissions:
      contents: read
    steps:
      - name: Checkout candidate implementation
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          repository: ${{ github.repository }}
          ref: ${{ github.sha }}
          path: candidate
          persist-credentials: false
          fetch-depth: 0

      - name: Checkout immutable candidate harness
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          repository: ${{ job.workflow_repository }}
          ref: ${{ job.workflow_sha }}
          path: harness
          persist-credentials: false
          fetch-depth: 1

      - name: Validate candidate harness identity
        shell: pwsh
        env:
          CALLER_REPOSITORY: ${{ github.repository }}
          HARNESS_REPOSITORY: ${{ job.workflow_repository }}
          HARNESS_SHA: ${{ job.workflow_sha }}
          CANDIDATE_SHA: ${{ github.sha }}
        run: |
          $ErrorActionPreference = 'Stop'
          if ($env:CALLER_REPOSITORY -ne 'AIAllTheThingz/Engineering-Standards') { throw 'Candidate validation is restricted to the Engineering Standards repository.' }
          if ($env:HARNESS_REPOSITORY -ne 'AIAllTheThingz/Engineering-Standards') { throw 'Unexpected candidate harness repository.' }
          if ($env:HARNESS_SHA -notmatch '^[a-fA-F0-9]{40}$') { throw 'Candidate harness must execute from an immutable full SHA.' }
          if ($env:CANDIDATE_SHA -notmatch '^[a-fA-F0-9]{40}$') { throw 'Candidate checkout must use an immutable full SHA.' }
          $head = (& git -C candidate rev-parse HEAD).Trim()
          if ($LASTEXITCODE -ne 0 -or $head -ne $env:CANDIDATE_SHA) { throw 'Candidate checkout HEAD does not match github.sha.' }
          $harnessHead = (& git -C harness rev-parse HEAD).Trim()
          if ($LASTEXITCODE -ne 0 -or $harnessHead -ne $env:HARNESS_SHA) { throw 'Candidate harness checkout HEAD does not match job.workflow_sha.' }

      - name: Set up pinned Python
        uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1
        with:
          python-version: 3.12.11

      - name: Set up pinned Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020
        with:
          node-version: 22.17.0

      - name: Set up pinned .NET
        uses: actions/setup-dotnet@a98b56852c35b8e3190ac28c8c2271da59106c68
        with:
          dotnet-version: 8.0.411

      - name: Install hash-verified PowerShell runtime
        shell: pwsh
        run: |
          pwsh -NoProfile -File harness/scripts/Install-ValidatorRuntime.ps1 `
            -RepositoryPath harness `
            -PackageCachePath (Join-Path $env:RUNNER_TEMP 'candidate-runtime-cache') `
            -InstallRoot (Join-Path $env:RUNNER_TEMP 'candidate-pwsh-7.4.11') `
            -EvidencePath (Join-Path $env:RUNNER_TEMP 'candidate-validator-evidence/runtime-bootstrap.json')

      - name: Install immutable candidate-validation dependencies
        shell: bash
        run: |
          set -euo pipefail
          "$VALIDATOR_PWSH" -NoProfile -File harness/scripts/Install-ValidatorDependencies.ps1 \
            -RepositoryPath harness \
            -PackageCachePath "$RUNNER_TEMP/candidate-package-cache" \
            -ModuleRoot "$RUNNER_TEMP/candidate-psmodules" \
            -PythonPackageRoot "$RUNNER_TEMP/candidate-python" \
            -ToolRoot "$RUNNER_TEMP/candidate-tools" \
            -EvidencePath "$RUNNER_TEMP/candidate-validator-evidence/dependencies.json" \
            -SbomPath "$RUNNER_TEMP/candidate-validator-evidence/validator-sbom.cdx.json" \
            -RuntimeEvidencePath "$RUNNER_TEMP/candidate-validator-evidence/runtime-bootstrap.json"

      - name: Run candidate implementation validation
        shell: pwsh
        working-directory: candidate
        env:
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.before || '' }}
          CALLER_REPOSITORY: ${{ github.repository }}
          CANDIDATE_SHA: ${{ github.sha }}
          HARNESS_SHA: ${{ job.workflow_sha }}
        run: |
          $ErrorActionPreference = 'Stop'
          $commandMarker = 'candidate-' + [guid]::NewGuid().ToString('N')
          Write-Output "::stop-commands::$commandMarker"
          try {
            $root = (Resolve-Path .).Path
            $reportRoot = Join-Path $env:RUNNER_TEMP 'candidate-validation'
            New-Item -ItemType Directory -Path $reportRoot -Force | Out-Null

            function Invoke-CandidateScript {
              param([Parameter(Mandatory)][string]$RelativePath, [string[]]$Arguments = @())
              & $env:VALIDATOR_PWSH -NoProfile -File (Join-Path $root $RelativePath) @Arguments
              if ($LASTEXITCODE -ne 0) { throw "Candidate command '$RelativePath' failed with exit code $LASTEXITCODE." }
            }

            $baseSha = $env:BASE_SHA
            if ($baseSha -notmatch '^[a-fA-F0-9]{40}$' -or $baseSha -match '^0{40}$') {
              $baseSha = (& git rev-parse "$($env:CANDIDATE_SHA)^" 2>$null).Trim()
            }
            if ($baseSha -notmatch '^[a-fA-F0-9]{40}$') { throw 'A valid base SHA is required for candidate diff validation.' }
            & git diff --check "$baseSha...$($env:CANDIDATE_SHA)"
            if ($LASTEXITCODE -ne 0) { throw 'Candidate git diff check failed.' }

            Invoke-CandidateScript 'scripts/Invoke-GovernanceValidation.ps1' @(
              '-Path',$root,
              '-EvidenceRoot',$reportRoot,
              '-CallerRepository',$env:CALLER_REPOSITORY,
              '-CallerCommitSha',$env:CANDIDATE_SHA,
              '-RepositoryOwnerType','User',
              '-ExpectedReusableWorkflowSha',$env:HARNESS_SHA,
              '-CandidateMaintainerValidation'
            )
          }
          finally {
            Write-Output "::$commandMarker::"
          }
