# Demo Skills

This directory is discoverable only from this isolated lab workspace and
is excluded from the production skill root. `bash-review` is a read-only demo;
deterministic validation does not certify live model behavior.
