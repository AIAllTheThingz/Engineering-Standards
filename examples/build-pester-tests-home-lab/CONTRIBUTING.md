# Contributing

Keep the lab synthetic, secret-free, deterministic, and isolated. New behavior
must have a requirement mapping plus positive and negative tests. Run
`pwsh -NoProfile -File examples/build-pester-tests-home-lab/tools/Test-Demo.ps1`
and report unavailable checks as `NotRun`.
