# Demo Skills

This directory is discoverable only when this home-lab example is opened as
its own workspace. `build-pester-tests` is an isolated, nonproduction skill;
deterministic checks do not certify live model behavior.
