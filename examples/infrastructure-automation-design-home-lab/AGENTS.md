# Infrastructure Automation Design Home Lab

This example inherits `agents/AGENTS_Base.md`,
`agents/AGENTS_Infrastructure.md`, and `agents/AGENTS_Integration.md`. Treat
central governance as read-only authority. Use fictional requirements only and
produce design artifacts without authentication, planning against live state,
or deployment. Limit writes to this example or test temporary storage. Live
model behavior and operational verification remain `NotRun`.
