# Contributing

Preserve the synthetic plan-only boundary. Add negative tests for any new gate
or failure mode. Run `pwsh -NoProfile -File
examples/safe-automation-home-lab/tools/Test-Demo.ps1` and record live checks as
`NotRun`.
