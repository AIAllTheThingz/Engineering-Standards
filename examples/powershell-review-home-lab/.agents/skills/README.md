# Demo Skills

This directory contains skills discoverable only when the home-lab example is opened as its own workspace. They are excluded from the Engineering Standards repository's production `.agents/skills` root.

`powershell-review` is a read-only demonstration skill. Its deterministic tests validate structure, routing fixtures, boundaries, and illustrative output contracts; they do not certify live model behavior.
