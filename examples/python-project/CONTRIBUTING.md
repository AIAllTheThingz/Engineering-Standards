# Contributing

Use CPython 3.12.11. Install `requirements-ci.lock` with `--require-hashes --only-binary=:all: --no-deps`, run `tools/Test-Example.ps1`, review the evidence, and classify risk before submitting changes.
