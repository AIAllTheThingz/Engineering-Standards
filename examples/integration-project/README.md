# Integration Example

This example demonstrates a governed integration repository shape using local contract fixtures only. It models REST/webhook style evidence without calling a real provider.

Run validation from the repository root:

```powershell
pwsh -NoProfile -File examples/integration-project/tools/Test-Example.ps1 -Path examples/integration-project
```
