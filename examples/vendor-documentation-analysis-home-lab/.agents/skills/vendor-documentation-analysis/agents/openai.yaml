interface:
  display_name: "Vendor Documentation Analysis"
  short_description: "Analyze versioned vendor guidance with citations"
  default_prompt: "Use $vendor-documentation-analysis to compare these synthetic vendor snapshots and cite every material claim."
policy:
  allow_implicit_invocation: true
