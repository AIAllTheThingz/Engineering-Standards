# Demo Skills

`networking` is discoverable only when this lab directory is opened as its own workspace. It is excluded from the Engineering Standards production `.agents/skills` root and is not production-certified.
