# Bash Project Agent Instructions

This functional example inherits:

- `agents/AGENTS_Base.md`
- `agents/AGENTS_Bash.md`

Keep the example synthetic, repository-relative, secret-free, non-networked,
and non-destructive. Functional Bash execution is permitted only through
`tools/Test-Example.ps1` locally or the dedicated governed Bash workflow.
