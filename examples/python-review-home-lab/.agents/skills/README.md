# Demo Skills

This directory is discoverable only when this home-lab example is opened as
its own workspace. It is excluded from the Engineering Standards production
skill root.

`python-review` is a read-only demonstration skill. Deterministic tests cover
package structure, routing fixtures, boundaries, and illustrative output; they
do not certify live model behavior.
