# Security

This nonproduction demonstration uses synthetic data only. Never add real
credentials, account identifiers, internal endpoints, state, plan files, or
production infrastructure. Never run Terraform/OpenTofu against the unsafe
sample. Deterministic validation does not authorize provider, backend, cloud,
or external access and does not certify live model behavior.
