# Demo Skills

This directory is discoverable only in this isolated home-lab workspace and is
excluded from production skill discovery. `terraform-review` is a read-only
demo; deterministic checks do not certify live model behavior.
